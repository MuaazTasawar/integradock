NEXT_PUBLIC_GO_ENGINE_URL=http://localhost:8080
NEXT_PUBLIC_PY_PLANNER_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws

# INSECURE FOR PRODUCTION: exposed to the browser bundle because this MVP
# calls go-engine directly from client code. Before any real deployment,
# move all go-engine/py-planner calls behind a Next.js Route Handler and
# keep this secret server-side only (regular env var, not NEXT_PUBLIC_).
INTERNAL_API_SECRET=integradock-dev-secret-8f3a2b91