# Server
PORT=8080
ENV=development

# Database
DATABASE_URL=postgres://integradock:integradock_dev_password@localhost:5432/integradock?sslmode=disable

# Redis
REDIS_URL=localhost:6379

# Python planner service
PY_PLANNER_URL=http://localhost:8000

# Auth (simple shared-secret for demo tenant; replace with real auth later)
INTERNAL_API_SECRET=REPLACE_WITH_RANDOM_SECRET_STRING